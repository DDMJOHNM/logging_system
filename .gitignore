# Accidental composer install at repo root (Laravel app lives in backend/)
/vendor/

# Terraform (keep state + provider cache out of git)
**/.terraform/
**/.terraform.lock.hcl
**/terraform.tfstate
**/terraform.tfstate.*
**/crash.log
**/crash.*.log
